import React from 'react';

const HomePage = () => {
  return (
    <div>
      <div>Fooo</div>
      <button onClick={() => console.log('foo')} >FOO</button>
    </div>
  );
};

export default {
  component: HomePage
};